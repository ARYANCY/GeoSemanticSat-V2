# External research sources boundary

This documentation refresh is intentionally source-based. No external paper, product, benchmark, or web claim is used as evidence for implemented behavior.

For repository behavior, use the Python and C# source, `docker-compose.yml`, `Dockerfile`, project files, test files, and generated FastAPI OpenAPI document. External literature may inform future design work, but it does not establish what this checkout implements.