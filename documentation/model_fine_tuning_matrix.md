# Model fine-tuning matrix: source-backed status

| Item | What the repository establishes |
|---|---|
| TerraMind, Prithvi, SatMAE++, GFM Composition | Python adapter modules and configuration paths exist. |
| Qwen | Backend configuration has a local model path and AI-oriented service modules. |
| Fine-tuned weights | Paths and a setting exist; installed artifacts and training results are not established. |
| ONNX | Desktop Engine references ONNX Runtime and contains `OnnxModelRunner`. |
| Offline behavior | Python configuration sets Hugging Face and Transformers offline environment variables. |

This replaces unverified hyperparameter, benchmark, and dataset claims with the repository facts above.