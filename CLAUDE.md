# CLAUDE.md

See **[AGENTS.md](./AGENTS.md)** — it holds all agent instructions for this
repository (project layout, how to run the Docker backend and the Avalonia desktop
UI, tests, and commit rules). Follow it directly; this file exists only to point
Claude Code at it.

Before running anything, also read `Jyotirmoy_SETUP.md` in the repo root if it is
present. It is the gitignored, machine-specific setup guide and it takes precedence
over `README.md` and `run.md`, which contain stale absolute paths from another
machine.
